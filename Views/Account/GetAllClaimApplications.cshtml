@model InuranceAssignmentAPD03.Models.ViewAllClaimsViewModel

@foreach (var item in Model.Claims)
{

    if (item.claim != null && item.claim.id > 1)
    {
        <partial name="_ClaimView" model="item" />
        }
    }